# Leave empty for local dev — Vite's dev server proxies /api and /uploads to
# http://localhost:3000 (see vite.config.js).
#
# When you deploy the backend somewhere (Render, Railway, etc.), set this to
# its full URL, e.g.:
# VITE_API_URL=https://your-backend.onrender.com
VITE_API_URL=
